{
  "name": "bulk_csv_pandadoc_mailer",
  "version": "1.0.0",
  "description": "Basic template for taking a CSV file with data, and merging that data into an existing PandaDoc template and then sending for signing",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Eric Leslie",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.4.0",
    "csv-parser": "^3.0.0"
  }
}
